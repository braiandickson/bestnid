<div class="col-md-6 col-md-offset-3">
  <div class="row">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h2>Registrarse</h2>
      </div>
	      <div class="panel-body">
	      	<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
				<%= f.error_notification %>
				  <div class="form-inputs">
				    <%= f.input :email, label: "Correo electronico:", placeholder: "Ej: nombre@dominio.com", required: true, autofocus: true %>
				    <%= f.input :password, label: "Contraseña:", required: true, hint: ("#{@minimum_password_length} caracteres minimo" if @validatable) %>
				    <%= f.input :password_confirmation, label: "Confirmar contraseña:", required: true %>
				  </div>

				  <div class="form-actions">
				    <%= f.button :submit, "Registrarse", class: "btn btn-primary" %>
				    <%= link_to "Cancelar", root_path, class: "btn btn-primary" %>
				  </div>
			<% end %>
	      </div>
		</div>
	</div>
</div>