<%= simple_form_for @product do |f| %>
	<%= f.input :name, label: 'Ingrese el nombre del producto a subastar' %>
	<%= f.input :description, label: 'Ingrese la descripción del producto' %>
	<%= f.input :due_date, label: 'Tiempo de subasta:' %>
	<%= f.button :submit, 'Crear Subasta'%>
<% end  %>